import { describe, it, expect } from 'vitest';

describe('OrdenForm', () => {
  it('submits valid form', () => {
    expect(true).toBe(true);
  });

  it('shows validation errors', () => {
    expect(true).toBe(true);
  });

  it('prevents negative cantidad', () => {
    expect(true).toBe(true);
  });
});